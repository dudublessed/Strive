import Foundation

/// SF Symbol names used across the app. Centralised so a symbol swap
/// is one edit instead of a codebase grep.
enum Icons {
    static let split = "bolt.fill"
    static let elevation = "mountain.2.fill"
    static let distance = "figure.run"
    static let duration = "clock.fill"
    static let pace = "speedometer"
    static let calendar = "calendar"
    static let importArchive = "square.and.arrow.down"
    static let dashboard = "trophy.fill"
    static let activities = "list.bullet.rectangle"
}
